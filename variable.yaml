- name: variable example
  hosts: all
  vars:
    url: "https://goloble.variable.com"
  vars_files:
    - extra_vars.yaml
  tasks:
    - name: print url variable
      debug:
        msg: "The URL is {{ url }}"

    - name: print url variable
      debug:
        msg: "The URL is {{ url }}"


    - name: print url variable
      debug:
        msg: "The URL is {{ url }}"
      vars:
        url: "https://local.variable.com"