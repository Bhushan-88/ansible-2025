- name: variable example
  hosts: all
  vars:
  #  url: "https://golobl.variable.com"
  #vars_files:
  #  - ./variables.txt
  #vars_prompt:
  #  - name: "url"
  #    prompt: "Please enter the URL"
  #    private: no 
  tasks:
    - name: print url variable
      debug:
        msg: "The URL is {{ url }}"

    - name: print url variable
      debug:
        msg: "The URL is {{ url }}"


    - name: print url variable
      debug:
        msg: "The URL is {{ url }}"
      vars:
        url: "https://local.variable.com"