- name: register example
  hosts: all

  tasks:

    - name: get hostname
      shell: hostname
      register: hostname_result
    - name: print hostname
      debug:
        msg: "The hostname is {{ hostname_result.stdout }}"