---
- name: Configure server based on OS
  hosts: all
  gather_facts: yes
  
  tasks:
    - name: Install web server on Ubuntu
      apt:
        name: apache2
      when: ansible_distribution == "Ubuntu"
    
    - name: Install web server on CentOS
      yum:
        name: httpd
      when: ansible_distribution == "CentOS"
